spring:
  jackson:
    time-zone: Asia/Seoul
  mvc:
    problemdetails:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  application:
    name: smartLedger
  datasource:
    url: jdbc:mysql://43.202.209.189:3306/smartledger_test?serverTimezone=Asia/Seoul&useSSL=false
    username: smartknu3707
    password: smartknu3707
    driver-class-name: com.mysql.cj.jdbc.Driver
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info   # health, info 엔드포인트만 외부 노출
  endpoint:
    health:
      show-details: when_authorized   # 인증된 사용자만 DB 상태 같은 세부정보 확인 가능
jwt:
  secret-key: '59abad1c1fe24ec91432ed2acf2859e12101f9f670f57feec185c60c8bf7ae57'
crypto:
  secret-key: 9Sfglzkf49X3zD3O
  init-vector: Khog/ugiJii+A3YH